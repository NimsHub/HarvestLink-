/* HarvestLink prototype: seeded state is persisted in localStorage for the browser session. */
const seed = {
  users: [
    { id: 'u1', name: 'Greenfield Organics', role: 'seller', location: 'Nuwara Eliya', phone: '+94 77 382 1951', email: 'hello@greenfield.lk', initials: 'GO', color: '#d4e5ad', rating: 4.9, reviews: 38 },
    { id: 'u2', name: 'Kandyan Harvest Co.', role: 'seller', location: 'Kandy', phone: '+94 76 490 2526', email: 'sales@kandyanharvest.lk', initials: 'KH', color: '#f0d4a9', rating: 4.7, reviews: 24 },
    { id: 'u3', name: 'Maya Trading House', role: 'customer', location: 'Colombo', phone: '+94 77 018 4623', email: 'buy@mayatrading.lk', initials: 'MT', color: '#e7c1a4', rating: 4.8, reviews: 16 },
    { id: 'u4', name: 'Oceanfront Kitchens', role: 'customer', location: 'Galle', phone: '+94 71 336 8047', email: 'orders@oceanfront.lk', initials: 'OK', color: '#bfd6ec', rating: 4.6, reviews: 12 },
    { id: 'u5', name: 'Swift Route Logistics', role: 'delivery', location: 'Colombo', phone: '+94 77 640 8551', email: 'dispatch@swiftroute.lk', initials: 'SR', color: '#d2c6ea', rating: 4.9, reviews: 52 },
    { id: 'u6', name: 'Hill Country Freight', role: 'delivery', location: 'Badulla', phone: '+94 75 801 2049', email: 'ops@hillcountry.lk', initials: 'HF', color: '#e7cfbb', rating: 4.8, reviews: 31 }
  ],
  products: [
    { id: 'p1', title: 'Premium Heirloom Tomatoes', category: 'Vegetables', sellerId: 'u1', basePrice: 340, currentPrice: 410, unit: 'kg', quantity: '320 kg', image: 'https://images.unsplash.com/photo-1546094096-0df4bcaaa337?auto=format&fit=crop&w=900&q=85', labels: ['Fresh', 'Local'], description: 'Sun-ripened heirloom tomatoes, hand-picked this morning. Great colour, firm texture, and a sweet balanced flavour for hotels and kitchens.', bids: [{buyerId:'u4',amount:390,time:'Today, 10:42'}, {buyerId:'u3',amount:410,time:'Today, 12:18'}], closes: 'Closes in 5h 24m', rating:4.8 },
    { id: 'p2', title: 'Ceylon Cinnamon Quills', category: 'Herbs & Spices', sellerId: 'u2', basePrice: 2200, currentPrice: 2550, unit: 'kg', quantity: '85 kg', image: 'https://images.unsplash.com/photo-1596040033229-a9821ebd058d?auto=format&fit=crop&w=900&q=85', labels: ['Export Grade', 'Organic'], description: 'Grade Alba Ceylon cinnamon quills from a single origin estate. Carefully packed to retain aroma and essential oil content.', bids: [{buyerId:'u3',amount:2550,time:'Today, 09:32'}], closes:'Closes in 1d 4h', rating:4.9 },
    { id: 'p3', title: 'Baby Spinach Leaves', category: 'Vegetables', sellerId: 'u1', basePrice: 520, currentPrice: 580, unit: 'kg', quantity: '60 kg', image: 'https://images.unsplash.com/photo-1576045057995-568f588f82fb?auto=format&fit=crop&w=900&q=85', labels: ['Fresh', 'Pesticide Free'], description: 'Tender baby spinach leaves washed and chilled at source. Available in 2 kg food-service packs.', bids: [{buyerId:'u4',amount:580,time:'Yesterday'}], closes:'Closes in 8h 11m', rating:4.7 },
    { id: 'p4', title: 'Hass Avocados', category: 'Fruit', sellerId: 'u2', basePrice: 690, currentPrice: 740, unit: 'kg', quantity: '180 kg', image: 'https://images.unsplash.com/photo-1523049673857-eb18f1d7b578?auto=format&fit=crop&w=900&q=85', labels: ['Fresh', 'Local'], description: 'Mature-green Hass avocados, graded for consistent size and ripening. Ready for retail or food service.', bids: [{buyerId:'u3',amount:740,time:'Today, 08:12'}], closes:'Closes in 15h 08m', rating:4.8 },
    { id: 'p5', title: 'Red Raw Rice', category: 'Grains', sellerId: 'u1', basePrice: 210, currentPrice: 245, unit: 'kg', quantity: '1,000 kg', image: 'https://images.unsplash.com/photo-1586208958839-06c17cacdf08?auto=format&fit=crop&w=900&q=85', labels: ['Local', 'Harvest 2026'], description: 'Nutritious unpolished red raw rice grown with responsible water practices. Supplied in 25 kg sacks.', bids: [{buyerId:'u4',amount:230,time:'Today, 08:41'},{buyerId:'u3',amount:245,time:'Today, 11:04'}], closes:'Closes in 2d 2h', rating:4.6 },
    { id: 'p6', title: 'King Coconuts', category: 'Fruit', sellerId: 'u2', basePrice: 115, currentPrice: 135, unit: 'each', quantity: '500 units', image: 'https://images.unsplash.com/photo-1560769686-5e72de1e3f4b?auto=format&fit=crop&w=900&q=85', labels: ['Fresh', 'Local'], description: 'Naturally sweet king coconuts with bright orange husks. Select-grade stock for events, resorts, and retail.', bids: [{buyerId:'u3',amount:135,time:'Today, 12:07'}], closes:'Closes in 18h 36m', rating:4.9 },
    { id: 'p7', title: 'Rainbow Carrots', category: 'Vegetables', sellerId: 'u1', basePrice: 380, currentPrice: 410, unit: 'kg', quantity: '140 kg', image: 'https://images.unsplash.com/photo-1447175008436-054170c2e979?auto=format&fit=crop&w=900&q=85', labels: ['Fresh', 'Pesticide Free'], description: 'Mixed purple, yellow, and orange carrots with their tops trimmed. Naturally colourful and crisp.', bids: [{buyerId:'u4',amount:410,time:'Yesterday'}], closes:'Closes in 6h 52m', rating:4.7 },
    { id: 'p8', title: 'Forest Honey', category: 'Pantry', sellerId: 'u2', basePrice: 1900, currentPrice: 1900, unit: 'kg', quantity: '48 kg', image: 'https://images.unsplash.com/photo-1587049352851-8d4e89133924?auto=format&fit=crop&w=900&q=85', labels: ['Wild Harvest', 'Raw'], description: 'Unheated raw forest honey with a full-bodied floral finish. Supplied in food-safe bulk tubs.', bids: [], closes:'Closes in 1d 12h', rating:4.8 },
    { id: 'p9', title: 'Butterhead Lettuce', category: 'Vegetables', sellerId: 'u1', basePrice: 190, currentPrice: 225, unit: 'head', quantity: '250 heads', image: 'https://images.unsplash.com/photo-1556801712-76c8eb07bbc9?auto=format&fit=crop&w=900&q=85', labels: ['Fresh', 'Local'], description: 'Soft, sweet butterhead lettuce harvested to order and cold-packed. A favourite for restaurants.', bids: [{buyerId:'u3',amount:225,time:'Today, 10:22'}], closes:'Closes in 11h 14m', rating:4.8 },
    { id: 'p10', title: 'Fresh Turmeric Root', category: 'Herbs & Spices', sellerId: 'u2', basePrice: 430, currentPrice: 470, unit: 'kg', quantity: '210 kg', image: 'https://images.unsplash.com/photo-1615485500834-bc10199bc727?auto=format&fit=crop&w=900&q=85', labels: ['Fresh', 'Local'], description: 'Bright, aromatic turmeric root cleaned and ready for processing, juicing, or culinary use.', bids: [{buyerId:'u4',amount:470,time:'Today, 09:15'}], closes:'Closes in 22h 05m', rating:4.7 }
  ],
  requirements: [
    {id:'r1', title:'Weekly leafy greens supply', category:'Vegetables', quantity:'90 kg / week', budget:590, buyerId:'u4', delivery:'Galle', deadline:'Sep 20, 2026', description:'Looking for a consistent mix of spinach, lettuce, and kale for a boutique hotel group.', bids:[{sellerId:'u1',amount:560, note:'Can supply mixed, chilled packs every Tuesday.'}]},
    {id:'r2', title:'Bulk organic spices', category:'Herbs & Spices', quantity:'120 kg', budget:2600, buyerId:'u3', delivery:'Colombo', deadline:'Sep 23, 2026', description:'Seeking certified organic cinnamon, turmeric, and pepper for a premium retail line.', bids:[]},
    {id:'r3', title:'Seasonal fruit box programme', category:'Fruit', quantity:'250 boxes', budget:1850, buyerId:'u4', delivery:'Galle', deadline:'Oct 01, 2026', description:'Varied local fruit boxes for guest welcome baskets. Need weekly replenishment.', bids:[]}
  ],
  reviews: [
    { id:'v1', productId:'p1', authorId:'u4', rating:5, text:'Excellent colour and delivery condition. Our kitchen team was very happy.', date:'2 days ago' },
    { id:'v2', productId:'p1', authorId:'u3', rating:4, text:'Beautiful tomatoes — consistent quality across the crates.', date:'1 week ago' },
    { id:'v3', productId:'p2', authorId:'u3', rating:5, text:'Perfectly rolled and very fragrant. Great export grade.', date:'3 days ago' }
  ],
  currentUserId:'u3'
};

const storageKey = 'harvestlink-prototype-v1';
let db = loadDb();
let state = { page: 'market', selectedProductId: null, search: '', category: '', seller: '' };
const app = document.getElementById('app');
const modalRoot = document.getElementById('modal-root');
const toastRoot = document.getElementById('toast-root');

function clone(v) { return JSON.parse(JSON.stringify(v)); }
function loadDb(){ try { return JSON.parse(localStorage.getItem(storageKey)) || clone(seed); } catch { return clone(seed); } }
function saveDb(){ localStorage.setItem(storageKey, JSON.stringify(db)); }
function currentUser(){ return db.users.find(u => u.id === db.currentUserId); }
function user(id){ return db.users.find(u => u.id === id) || {name:'Unknown user', initials:'?', rating:0}; }
function money(value){ return `LKR ${Number(value).toLocaleString()}`; }
function avatar(person, cls=''){ return `<span class="avatar ${cls}" style="background:${person.color || '#d8e5c1'}">${person.initials}</span>`; }
function escapeHtml(input){ return String(input ?? '').replace(/[&<>'"]/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[char])); }
function stars(rating){ return `<span class="stars" aria-label="${rating} out of 5 stars">${'★'.repeat(Math.round(rating))}${'☆'.repeat(5-Math.round(rating))}</span>`; }
function isSeller(){ return currentUser().role === 'seller'; }
function isCustomer(){ return currentUser().role === 'customer'; }
function isDelivery(){ return currentUser().role === 'delivery'; }
function toast(title, detail=''){ const el=document.createElement('div'); el.className='toast'; el.innerHTML=`<strong>${escapeHtml(title)}</strong>${detail?`<small>${escapeHtml(detail)}</small>`:''}`; toastRoot.append(el); setTimeout(()=>el.remove(),3500); }
function setPage(page, id=null){ state.page=page; state.selectedProductId=id; window.location.hash=id?`product/${id}`:page; render(); window.scrollTo({top:0,behavior:'smooth'}); }

function productCard(product){ const seller=user(product.sellerId); return `<a class="product-card" href="#product/${product.id}" data-product="${product.id}" aria-label="Open ${escapeHtml(product.title)}">
  <div class="product-image-wrap"><img class="product-image" src="${product.image}" alt="${escapeHtml(product.title)}" loading="lazy" />
    <div class="certs">${product.labels.slice(0,2).map(label=>`<span class="cert">${escapeHtml(label)}</span>`).join('')}</div></div>
  <div class="product-body"><div class="category">${escapeHtml(product.category)}</div><div class="product-title">${escapeHtml(product.title)}</div>
    <div class="seller-line">Sold by ${escapeHtml(seller.name)}</div><div class="price-row"><div><small>Current bid</small><strong>${money(product.currentPrice)}<small>/ ${escapeHtml(product.unit)}</small></strong></div><div class="bid-count">${product.bids.length} bid${product.bids.length===1?'':'s'}<br>${escapeHtml(product.quantity)}</div></div></div>
</a>`; }

function header(){ const me=currentUser(); const nav = [ ['market','Marketplace'], ['requirements','Supply needs'], ['dashboard', isSeller()?'My listings':isDelivery()?'Delivery board':'My activity'] ]; return `<div class="topline"><div class="shell"><span>Trusted B2B sourcing for Sri Lanka</span><span>↗ Verified producers & buyers</span></div></div><header class="appbar"><div class="shell"><a class="brand" href="#market" data-nav="market"><span class="brand-mark">⌁</span><span>HarvestLink</span></a><nav class="nav" id="main-nav" aria-label="Main navigation">${nav.map(([id,label])=>`<button class="nav-btn ${state.page===id?'active':''}" data-nav="${id}">${label}</button>`).join('')}</nav><button class="mobile-nav-toggle" id="mobile-nav" aria-label="Open navigation">☰</button><div class="account-menu"><button class="account-trigger" id="account-button" aria-label="Open account options">${avatar(me)}<span class="account-copy"><strong>${escapeHtml(me.name)}</strong><small>${escapeHtml(me.role)} account · ▾</small></span></button></div></div></header>`; }

function home(){ const featured=db.products.slice(0,4); return `<main class="main shell"><section class="hero"><div class="eyebrow">A better way to trade</div><h1>Direct from trusted growers to growing businesses.</h1><p>Discover fresh agricultural products, make competitive bids, and build reliable supply relationships.</p><div class="hero-actions"><button class="btn btn-primary" data-nav="market">Browse marketplace</button>${isCustomer()?'<button class="btn btn-secondary" data-modal="requirement">Post a supply need</button>':isSeller()?'<button class="btn btn-secondary" data-modal="product">Publish a product</button>':'<button class="btn btn-secondary" data-nav="dashboard">View delivery board</button>'}</div></section><section class="section-head"><div><h2>Fresh opportunities</h2><div class="panel-sub">Available right now from verified local producers</div></div><button class="linkish" data-nav="market">See all products →</button></section><div class="product-grid">${featured.map(productCard).join('')}</div><section class="two-col" style="margin-top:34px"><div class="panel"><h2>Trade with confidence</h2><p class="panel-sub">Every listing keeps the essentials clear: source, current price, condition, and certification.</p><div class="feature-list"><div class="feature-item"><span class="feature-icon">✓</span><div><strong>Transparent bidding</strong><small>See the live bid price before you respond.</small></div></div><div class="feature-item"><span class="feature-icon">⌘</span><div><strong>Buy what you need</strong><small>Post supply requirements and receive producer bids.</small></div></div><div class="feature-item"><span class="feature-icon">☆</span><div><strong>Built on reputation</strong><small>Rate products and trading partners after each deal.</small></div></div></div></div><div class="profile-card">${avatar(currentUser())}<h2>${escapeHtml(currentUser().name)}</h2><p>Your ${escapeHtml(currentUser().role)} workspace is ready.</p><div class="mini-stats"><div><strong>${db.products.length}</strong><small>Live lots</small></div><div><strong>${db.requirements.length}</strong><small>Supply needs</small></div><div><strong>${currentUser().rating} ★</strong><small>Account rating</small></div></div></div></section></main>`; }

function filteredProducts(){ return db.products.filter(p=>{ const seller=user(p.sellerId); return (!state.search || [p.title,p.description,p.category,seller.name].join(' ').toLowerCase().includes(state.search.toLowerCase())) && (!state.category || p.category===state.category) && (!state.seller || p.sellerId===state.seller); }); }
function marketplaceResults(){ const matches=filteredProducts(); return { count: `${matches.length} product${matches.length===1?'':'s'} available`, cards: matches.length?matches.map(productCard).join(''):'<div class="empty" style="grid-column:1/-1">No products match those filters. Try a broader search.</div>' }; }
function refreshMarketplaceResults(){ const results=marketplaceResults(); const count=document.getElementById('results-line'); const grid=document.getElementById('product-grid'); if(count)count.textContent=results.count; if(grid)grid.innerHTML=results.cards; }
function market(){ const categories=[...new Set(db.products.map(p=>p.category))]; const sellers=db.users.filter(u=>u.role==='seller'); const results=marketplaceResults(); return `<main class="main shell"><div class="page-heading"><div><h1>Marketplace</h1><p>Source directly from verified local agricultural producers.</p></div>${isSeller()?'<button class="btn btn-primary" data-modal="product">+ Publish product</button>':''}</div><div class="filterbar"><div class="search-box"><span>⌕</span><input id="search" value="${escapeHtml(state.search)}" placeholder="Search products, categories, or sellers" aria-label="Search products" /></div><div class="field"><label for="category-filter">Category</label><select id="category-filter"><option value="">All categories</option>${categories.map(c=>`<option value="${escapeHtml(c)}" ${state.category===c?'selected':''}>${escapeHtml(c)}</option>`).join('')}</select></div><div class="field"><label for="seller-filter">Seller</label><select id="seller-filter"><option value="">All sellers</option>${sellers.map(s=>`<option value="${s.id}" ${state.seller===s.id?'selected':''}>${escapeHtml(s.name)}</option>`).join('')}</select></div><button class="btn btn-secondary" id="clear-filters">Clear</button></div><div class="results-line" id="results-line">${results.count}</div><section class="product-grid" id="product-grid" style="margin-top:15px">${results.cards}</section></main>`; }

function detail(){ const product=db.products.find(p=>p.id===state.selectedProductId); if(!product){return market();} const seller=user(product.sellerId); const reviews=db.reviews.filter(r=>r.productId===product.id); const canBid=isCustomer(); return `<main class="main shell"><button class="detail-back" data-nav="market">← Back to marketplace</button><section class="detail-grid"><div><img class="detail-image" src="${product.image}" alt="${escapeHtml(product.title)}" /><div class="certs" style="position:static;margin-top:11px">${product.labels.map(label=>`<span class="cert" style="background:var(--sage)">${escapeHtml(label)}</span>`).join('')}</div></div><div class="detail-info"><div class="category">${escapeHtml(product.category)} · ${escapeHtml(product.quantity)} available</div><h1>${escapeHtml(product.title)}</h1><p class="detail-desc">${escapeHtml(product.description)}</p><div class="seller-card">${avatar(seller)}<div><strong>${escapeHtml(seller.name)}</strong><span>${escapeHtml(seller.location)} · Verified seller</span></div><div class="ratings">${stars(seller.rating)}<br><span style="color:var(--muted)">${seller.rating} · ${seller.reviews} reviews</span></div></div><div class="price-box"><div><small>Current highest bid</small><strong>${money(product.currentPrice)} <span style="font-family:DM Sans;font-size:14px">/ ${escapeHtml(product.unit)}</span></strong><small>Base price ${money(product.basePrice)} / ${escapeHtml(product.unit)}</small></div><span class="timer">◷ ${escapeHtml(product.closes)}</span></div><div class="detail-actions">${canBid?`<button class="btn btn-primary" data-modal="bid" data-product-id="${product.id}">Place a bid</button>`:'<button class="btn btn-primary" data-modal="auth">Join as a customer to bid</button>'}<button class="btn btn-secondary" data-modal="review" data-product-id="${product.id}">☆ Rate & review</button></div><section class="bid-log"><h2>Bid activity</h2>${product.bids.length?product.bids.slice().reverse().map(b=>{const buyer=user(b.buyerId);return `<div class="bid-row"><div><strong>${escapeHtml(buyer.name)}</strong><small>${escapeHtml(b.time)}</small></div><strong>${money(b.amount)} / ${escapeHtml(product.unit)}</strong></div>`}).join(''):'<p class="panel-sub">No bids yet. Be the first to make an offer.</p>'}</section></div></section><section class="reviews"><div class="section-head"><div><h2>Product reviews</h2><div class="panel-sub">Trade feedback from verified platform members</div></div><button class="linkish" data-modal="review" data-product-id="${product.id}">Write a review →</button></div>${reviews.length?reviews.map(review=>{const author=user(review.authorId);return `<div class="review"><div style="display:flex;gap:10px">${avatar(author)}<div><strong>${escapeHtml(author.name)}</strong><small>${escapeHtml(review.date)}</small><p>${escapeHtml(review.text)}</p></div></div><div>${stars(review.rating)}</div></div>`}).join(''):'<div class="empty">No reviews yet. Trade with this seller and share your experience.</div>'}</section></main>`; }

function requirementCard(req, mode='customer'){ const buyer=user(req.buyerId); const action=isSeller()?'View need & make offer →':mode==='customer' && req.buyerId===currentUser().id?'View supplier offers →':'View requirement →'; return `<article class="requirement-card is-clickable" data-requirement-detail="${req.id}" role="button" tabindex="0" aria-label="Open supply need: ${escapeHtml(req.title)}"><span class="status">Open requirement</span><h3>${escapeHtml(req.title)}</h3><p>${escapeHtml(req.description)}</p><div class="req-meta"><span>▣ ${escapeHtml(req.category)}</span><span>◫ ${escapeHtml(req.quantity)}</span><span>Budget up to <strong>${money(req.budget)}</strong></span><span>⌖ ${escapeHtml(req.delivery)}</span></div><div class="requirement-card-foot"><small>Posted by ${escapeHtml(buyer.name)} · ${req.bids.length} supplier bid${req.bids.length===1?'':'s'}</small><span class="requirement-card-action">${action}</span></div></article>`; }

function requirements(){ const visible=isCustomer()?db.requirements.filter(r=>r.buyerId===currentUser().id):db.requirements; return `<main class="main shell"><div class="page-heading"><div><h1>${isSeller()?'Buyer supply needs':isDelivery()?'Delivery-linked supply needs':'Your supply needs'}</h1><p>${isSeller()?'Respond with offers for buyers actively looking for stock.':isDelivery()?'See upcoming product demand across the platform.':'Post exactly what you need and compare producer offers in one place.'}</p></div>${isCustomer()?'<button class="btn btn-primary" data-modal="requirement">+ Post supply need</button>':''}</div><section class="two-col"><div>${visible.length?visible.map(r=>requirementCard(r)).join(''):'<div class="empty">You have not posted a supply need yet.<br><button class="btn btn-primary" style="margin-top:13px" data-modal="requirement">Post your first need</button></div>'}</div><aside class="panel"><h2>${isSeller()?'Turn demand into a deal':'How supply needs work'}</h2><p class="panel-sub">${isSeller()?'Review buyer budgets and quantities, then make a clear offer.':'A simple request lets local producers bring their best offer to you.'}</p><div class="feature-list">${isSeller()?'<div class="feature-item"><span class="feature-icon">1</span><div><strong>Review the brief</strong><small>Check volume, budget, location, and deadline.</small></div></div><div class="feature-item"><span class="feature-icon">2</span><div><strong>Submit your offer</strong><small>Set a price and add your supply note.</small></div></div><div class="feature-item"><span class="feature-icon">3</span><div><strong>Build the relationship</strong><small>Win repeat trade with reliable fulfilment.</small></div></div>':'<div class="feature-item"><span class="feature-icon">1</span><div><strong>Describe your need</strong><small>Choose a category, amount, and target price.</small></div></div><div class="feature-item"><span class="feature-icon">2</span><div><strong>Receive supplier bids</strong><small>Verified sellers respond directly with offers.</small></div></div><div class="feature-item"><span class="feature-icon">3</span><div><strong>Choose with confidence</strong><small>Use ratings and reviews before confirming.</small></div></div>'}</div></aside></section></main>`; }

function dashboard(){ const me=currentUser(); if(isSeller()){ const own=db.products.filter(p=>p.sellerId===me.id); return `<main class="main shell"><div class="page-heading"><div><h1>My listings</h1><p>Manage your published product lots and incoming buyer interest.</p></div><button class="btn btn-primary" data-modal="product">+ Publish product</button></div><div class="stat-grid"><div class="stat"><span>Active listings</span><strong>${own.length}</strong></div><div class="stat"><span>Current bid value</span><strong>${money(own.reduce((sum,p)=>sum+p.currentPrice,0))}</strong></div><div class="stat"><span>Buyer bids received</span><strong>${own.reduce((sum,p)=>sum+p.bids.length,0)}</strong></div></div><section class="panel" style="margin-top:24px"><h2>Published products</h2><p class="panel-sub">Open a listing to view and manage bid activity.</p>${own.map(p=>`<a href="#product/${p.id}" data-product="${p.id}" class="listing-row"><img src="${p.image}" alt="" /><div><strong>${escapeHtml(p.title)}</strong><small>${escapeHtml(p.quantity)} · ${p.bids.length} buyer bid${p.bids.length===1?'':'s'}</small></div><div class="listing-price"><small>Highest bid</small><strong>${money(p.currentPrice)}</strong></div><button class="btn btn-secondary">View →</button></a>`).join('')}</section></main>`; }
  if(isDelivery()){ return `<main class="main shell"><div class="page-heading"><div><h1>Delivery board</h1><p>Coordinate reliable farm-to-business movement.</p></div><button class="btn btn-primary" data-modal="auth">Update availability</button></div><div class="stat-grid"><div class="stat"><span>Ready pickups</span><strong>4</strong></div><div class="stat"><span>Open routes</span><strong>2</strong></div><div class="stat"><span>Service rating</span><strong>${me.rating} ★</strong></div></div><section class="two-col" style="margin-top:24px"><div class="panel"><h2>Available collection windows</h2><p class="panel-sub">Product lots ready for a logistics partner.</p>${db.products.slice(0,4).map(p=>`<div class="listing-row"><img src="${p.image}" alt="" /><div><strong>${escapeHtml(p.title)}</strong><small>${escapeHtml(user(p.sellerId).location)} → buyer delivery on request</small></div><div class="listing-price"><small>Ready stock</small><strong>${escapeHtml(p.quantity)}</strong></div></div>`).join('')}</div><div class="profile-card">${avatar(me)}<h2>Route-ready</h2><p>Keep your availability current so buyers and sellers can find the right delivery partner.</p><div class="mini-stats"><div><strong>12</strong><small>Completed runs</small></div><div><strong>98%</strong><small>On-time rate</small></div></div></div></section></main>`; }
  const bids=db.products.filter(p=>p.bids.some(b=>b.buyerId===me.id)); const needs=db.requirements.filter(r=>r.buyerId===me.id); return `<main class="main shell"><div class="page-heading"><div><h1>My activity</h1><p>Track your bids, requirements, and active sourcing.</p></div><button class="btn btn-primary" data-modal="requirement">+ Post supply need</button></div><div class="stat-grid"><div class="stat"><span>Active product bids</span><strong>${bids.length}</strong></div><div class="stat"><span>Open supply needs</span><strong>${needs.length}</strong></div><div class="stat"><span>Account rating</span><strong>${me.rating} ★</strong></div></div><section class="two-col" style="margin-top:24px"><div class="panel"><h2>Products you're bidding on</h2><p class="panel-sub">Your offers remain visible until the listing closes.</p>${bids.length?bids.map(p=>`<a href="#product/${p.id}" data-product="${p.id}" class="listing-row"><img src="${p.image}" alt="" /><div><strong>${escapeHtml(p.title)}</strong><small>Your bid ${money(p.bids.filter(b=>b.buyerId===me.id).at(-1).amount)} / ${p.unit}</small></div><div class="listing-price"><small>Live price</small><strong>${money(p.currentPrice)}</strong></div></a>`).join(''):'<div class="empty">No active bids yet. Browse the marketplace to find stock.</div>'}</div><div class="panel"><h2>Your supply needs</h2><p class="panel-sub">See the offers producers have made on your requests.</p>${needs.length?needs.map(r=>requirementCard(r,'customer')).join(''):'<div class="empty">No supply needs yet.</div>'}</div></section></main>`; }

function render(){ let content=state.page==='home'?home():state.page==='market'?market():state.page==='product'?detail():state.page==='requirements'?requirements():dashboard(); app.innerHTML=header()+content; attachEvents(); }

function openModal(type, dataId=null){ const me=currentUser(); if(type==='product'){ if(!isSeller()){ openModal('auth'); return; } modalRoot.innerHTML=`<div class="modal-overlay" data-close-modal><form class="modal modal-wide" id="product-form"><div class="modal-head"><div><h2>Publish a product</h2><p>Create a product listing that buyers can discover and bid on.</p></div><button class="close" type="button" data-close-modal aria-label="Close">×</button></div><div class="form-grid"><div class="field"><label>Product title</label><input required name="title" placeholder="e.g. Fresh mangoes" /></div><div class="field"><label>Category</label><select required name="category"><option value="Vegetables">Vegetables</option><option value="Fruit">Fruit</option><option value="Grains">Grains</option><option value="Herbs & Spices">Herbs & Spices</option><option value="Pantry">Pantry</option></select></div><div class="field span-2"><label>Description</label><textarea required name="description" placeholder="Describe grade, growing method, condition, and packing."></textarea></div><div class="field"><label>Base price (LKR / unit)</label><input required type="number" min="1" name="basePrice" placeholder="0" /></div><div class="field"><label>Available quantity</label><input required name="quantity" placeholder="e.g. 250 kg" /></div><div class="field"><label>Unit</label><select name="unit"><option value="kg">kg</option><option value="each">each</option><option value="box">box</option><option value="crate">crate</option></select></div><div class="field"><label>Image URL <span style="font-weight:400">(optional)</span></label><input name="image" placeholder="https://..." /></div><div class="field span-2"><label>Certification labels</label><input name="labels" placeholder="Fresh, Local, Organic" value="Fresh, Local" /></div></div><div class="modal-foot"><button class="btn btn-secondary" type="button" data-close-modal>Cancel</button><button class="btn btn-primary" type="submit">Publish listing</button></div></form></div>`; }
  else if(type==='bid'){ const product=db.products.find(p=>p.id===dataId); if(!product) return; const min=product.currentPrice+5; modalRoot.innerHTML=`<div class="modal-overlay" data-close-modal><form class="modal" id="bid-form" data-product-id="${product.id}"><div class="modal-head"><div><h2>Place a bid</h2><p>Your offer will be visible to the seller and competing buyers.</p></div><button class="close" type="button" data-close-modal>×</button></div><div class="panel" style="padding:14px"><strong>${escapeHtml(product.title)}</strong><div class="price-row" style="padding-top:8px"><span class="panel-sub" style="margin:0">Current bid</span><strong>${money(product.currentPrice)} / ${product.unit}</strong></div></div><div class="field" style="margin-top:15px"><label>Your bid (LKR / ${escapeHtml(product.unit)})</label><input name="amount" type="number" min="${min}" value="${min}" required /></div><p class="helper">Minimum next bid is ${money(min)} / ${escapeHtml(product.unit)}.</p><div class="modal-foot"><button class="btn btn-secondary" type="button" data-close-modal>Cancel</button><button class="btn btn-primary" type="submit">Confirm bid</button></div></form></div>`; }
  else if(type==='requirement'){ if(!isCustomer()){ openModal('auth'); return; } modalRoot.innerHTML=`<div class="modal-overlay" data-close-modal><form class="modal modal-wide" id="requirement-form"><div class="modal-head"><div><h2>Post a supply need</h2><p>Give producers a clear brief so they can send their strongest offer.</p></div><button class="close" type="button" data-close-modal>×</button></div><div class="form-grid"><div class="field"><label>What do you need?</label><input name="title" required placeholder="e.g. Weekly leafy greens supply" /></div><div class="field"><label>Category</label><select name="category"><option>Vegetables</option><option>Fruit</option><option>Grains</option><option>Herbs & Spices</option><option>Pantry</option></select></div><div class="field"><label>Quantity</label><input name="quantity" required placeholder="e.g. 100 kg / week" /></div><div class="field"><label>Maximum budget (LKR / unit)</label><input name="budget" type="number" min="1" required placeholder="0" /></div><div class="field"><label>Delivery location</label><input name="delivery" required placeholder="e.g. Colombo" /></div><div class="field"><label>Needed by</label><input name="deadline" type="date" required /></div><div class="field span-2"><label>Requirement details</label><textarea name="description" required placeholder="Describe quality, certification, packing, delivery, and any regular-supply needs."></textarea></div></div><div class="modal-foot"><button class="btn btn-secondary" type="button" data-close-modal>Cancel</button><button class="btn btn-primary" type="submit">Post requirement</button></div></form></div>`; }
  else if(type==='requirement-detail'){ const req=db.requirements.find(r=>r.id===dataId); if(!req) return; const buyer=user(req.buyerId); const offers=req.bids.map(b=>{const seller=user(b.sellerId);return `<div class="bid-row"><div><strong>${escapeHtml(seller.name)}</strong><small>${escapeHtml(b.note || 'No supply note added')}</small></div><strong>${money(b.amount)} / unit</strong></div>`;}).join(''); const canSeeOffers=isCustomer() && req.buyerId===me.id; const sellerForm=isSeller()?`<form id="requirement-bid-form" data-requirement-id="${req.id}" style="margin-top:18px"><h3 style="font-size:22px;margin-bottom:5px">Make your offer</h3><p class="panel-sub">Set a competitive price and explain how you can fulfil this supply need.</p><div class="field"><label>Your price (LKR / unit)</label><input name="amount" type="number" min="1" value="${req.budget}" required /></div><div class="field" style="margin-top:12px"><label>Supply note</label><textarea name="note" required placeholder="Describe your product quality, available quantity, and delivery plan."></textarea></div><div class="modal-foot"><button class="btn btn-secondary" type="button" data-close-modal>Cancel</button><button class="btn btn-primary" type="submit">Send supplier offer</button></div></form>`:''; modalRoot.innerHTML=`<div class="modal-overlay" data-close-modal><section class="modal modal-wide" aria-label="Supply need details"><div class="modal-head"><div><span class="status">Open supply need</span><h2 style="margin-top:7px">${escapeHtml(req.title)}</h2><p>Posted by ${escapeHtml(buyer.name)} · Needed by ${escapeHtml(req.deadline)}</p></div><button class="close" type="button" data-close-modal aria-label="Close">×</button></div><p style="color:var(--muted);margin-bottom:16px">${escapeHtml(req.description)}</p><div class="price-box"><div><small>Maximum budget</small><strong>${money(req.budget)} <span style="font-family:DM Sans;font-size:14px">/ unit</span></strong><small>${escapeHtml(req.quantity)} requested · Delivery to ${escapeHtml(req.delivery)}</small></div><span class="timer">▣ ${escapeHtml(req.category)}</span></div>${canSeeOffers?`<section class="bid-log"><h3 style="font-size:22px;margin-bottom:5px">Supplier offers</h3><p class="panel-sub">Compare pricing and supply notes from interested sellers.</p>${offers||'<p class="helper">No supplier offers yet.</p>'}</section>`:isSeller()?'<p class="helper" style="margin-top:16px">Your price will be sent directly to the buyer. Competing supplier prices are kept private.</p>':`<p class="helper" style="margin-top:16px">${req.bids.length} supplier offer${req.bids.length===1?'':'s'} received for this requirement.</p>`}${sellerForm}</section></div>`; }
  else if(type==='requirement-bid'){ const req=db.requirements.find(r=>r.id===dataId); modalRoot.innerHTML=`<div class="modal-overlay" data-close-modal><form class="modal" id="requirement-bid-form" data-requirement-id="${req.id}"><div class="modal-head"><div><h2>Respond with an offer</h2><p>${escapeHtml(req.title)} · ${escapeHtml(req.quantity)}</p></div><button class="close" type="button" data-close-modal>×</button></div><div class="field"><label>Your price (LKR / unit)</label><input name="amount" type="number" min="1" value="${req.budget}" required /></div><div class="field" style="margin-top:12px"><label>Supply note</label><textarea name="note" required placeholder="Explain product quality, availability, and delivery plan."></textarea></div><div class="modal-foot"><button class="btn btn-secondary" type="button" data-close-modal>Cancel</button><button class="btn btn-primary" type="submit">Send offer</button></div></form></div>`; }
  else if(type==='review'){ const product=db.products.find(p=>p.id===dataId); modalRoot.innerHTML=`<div class="modal-overlay" data-close-modal><form class="modal" id="review-form" data-product-id="${dataId}"><div class="modal-head"><div><h2>Rate this trade</h2><p>Your feedback helps buyers and sellers trade with confidence.</p></div><button class="close" type="button" data-close-modal>×</button></div><div class="field"><label>Review type</label><select name="reviewType"><option value="product">Product quality — ${escapeHtml(product.title)}</option><option value="seller">Seller experience — ${escapeHtml(user(product.sellerId).name)}</option></select></div><div class="field" style="margin-top:12px"><label>Rating</label><select name="rating"><option value="5">★★★★★ — Excellent</option><option value="4">★★★★☆ — Good</option><option value="3">★★★☆☆ — Okay</option><option value="2">★★☆☆☆ — Below expectations</option><option value="1">★☆☆☆☆ — Poor</option></select></div><div class="field" style="margin-top:12px"><label>Your review</label><textarea name="text" required placeholder="Share details that will help the next trading partner."></textarea></div><div class="modal-foot"><button class="btn btn-secondary" type="button" data-close-modal>Cancel</button><button class="btn btn-primary" type="submit">Publish review</button></div></form></div>`; }
  else if(type==='account'){ modalRoot.innerHTML=`<div class="modal-overlay" data-close-modal><div class="modal"><div class="modal-head"><div><h2>Account</h2><p>Switch a sample account or create a new one.</p></div><button class="close" type="button" data-close-modal>×</button></div><div class="field"><label>Prototype account</label><select id="account-switch">${db.users.map(u=>`<option value="${u.id}" ${u.id===me.id?'selected':''}>${u.name} — ${u.role}</option>`).join('')}</select></div><div class="modal-foot"><button class="btn btn-secondary" type="button" data-close-modal>Close</button><button class="btn btn-primary" data-modal="auth">Create account</button></div></div></div>`; }
  else { modalRoot.innerHTML=`<div class="modal-overlay" data-close-modal><form class="modal" id="auth-form"><div class="modal-head"><div><h2>Create your account</h2><p>Join HarvestLink as a seller, customer, or delivery partner.</p></div><button class="close" type="button" data-close-modal>×</button></div><div class="field"><label>Account type</label><div class="role-picker" id="role-picker"><button type="button" class="role-option selected" data-role="customer"><strong>Customer</strong><small>Source & bid</small></button><button type="button" class="role-option" data-role="seller"><strong>Seller</strong><small>List & offer</small></button><button type="button" class="role-option" data-role="delivery"><strong>Delivery</strong><small>Move goods</small></button></div><input type="hidden" name="role" value="customer" /></div><div class="field" style="margin-top:14px"><label>Business name</label><input name="name" required placeholder="Your business name" /></div><div class="field" style="margin-top:12px"><label>Email or phone number</label><input name="contact" required placeholder="name@business.com or +94 77 000 0000" /></div><p class="helper">Use either your business email or phone number to get started.</p><div class="modal-foot"><button class="btn btn-secondary" type="button" data-close-modal>Maybe later</button><button class="btn btn-primary" type="submit">Create account</button></div></form></div>`; }
  bindModalEvents(); }

function closeModal(){ modalRoot.innerHTML=''; }
function bindModalEvents(){ modalRoot.querySelectorAll('[data-close-modal]:not(.modal-overlay)').forEach(el=>el.addEventListener('click',closeModal)); const overlay=modalRoot.querySelector('.modal-overlay'); if(overlay) overlay.addEventListener('click',e=>{if(e.target===overlay)closeModal();}); modalRoot.querySelectorAll('[data-modal]').forEach(el=>el.addEventListener('click',e=>{e.preventDefault();openModal(el.dataset.modal,el.dataset.productId||el.dataset.requirementId||null);})); const rolePicker=document.getElementById('role-picker'); if(rolePicker)rolePicker.addEventListener('click',e=>{const button=e.target.closest('[data-role]');if(!button)return; rolePicker.querySelectorAll('.role-option').forEach(x=>x.classList.remove('selected'));button.classList.add('selected');document.querySelector('#auth-form [name=role]').value=button.dataset.role;}); const switcher=document.getElementById('account-switch'); if(switcher)switcher.addEventListener('change',e=>{db.currentUserId=e.target.value;saveDb();closeModal();render();toast('Account switched', `You are previewing ${currentUser().role} tools.`);});
  const productForm=document.getElementById('product-form'); if(productForm)productForm.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(productForm);const price=Number(f.get('basePrice'));const title=f.get('title').trim(); db.products.unshift({id:`p${Date.now()}`,title,category:f.get('category'),sellerId:currentUser().id,basePrice:price,currentPrice:price,unit:f.get('unit'),quantity:f.get('quantity'),image:f.get('image').trim()||'https://images.unsplash.com/photo-1464226184884-fa280b87c399?auto=format&fit=crop&w=900&q=85',labels:f.get('labels').split(',').map(x=>x.trim()).filter(Boolean).slice(0,3),description:f.get('description'),bids:[],closes:'Closes in 2d 0h',rating:5});saveDb();closeModal();state.page='dashboard';render();toast('Product published', `${title} is now visible in the marketplace.`);});
  const bidForm=document.getElementById('bid-form'); if(bidForm)bidForm.addEventListener('submit',e=>{e.preventDefault();const p=db.products.find(x=>x.id===bidForm.dataset.productId);const amount=Number(new FormData(bidForm).get('amount'));if(amount<=p.currentPrice){toast('Bid needs to be higher', `Enter more than ${money(p.currentPrice)}.`);return;}p.currentPrice=amount;p.bids.push({buyerId:currentUser().id,amount,time:'Just now'});saveDb();closeModal();render();toast('Bid placed', `Your offer of ${money(amount)} is now the current highest bid.`);});
  const reqForm=document.getElementById('requirement-form'); if(reqForm)reqForm.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(reqForm);const title=f.get('title').trim();db.requirements.unshift({id:`r${Date.now()}`,title,category:f.get('category'),quantity:f.get('quantity'),budget:Number(f.get('budget')),buyerId:currentUser().id,delivery:f.get('delivery'),deadline:new Date(f.get('deadline')).toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'}),description:f.get('description'),bids:[]});saveDb();closeModal();state.page='requirements';render();toast('Supply need posted', 'Verified sellers can now submit offers.');});
  const reqBid=document.getElementById('requirement-bid-form'); if(reqBid)reqBid.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(reqBid);const r=db.requirements.find(x=>x.id===reqBid.dataset.requirementId);r.bids.push({sellerId:currentUser().id,amount:Number(f.get('amount')),note:f.get('note')});saveDb();closeModal();render();toast('Offer sent', 'The buyer can now review your supply bid.');});
  const reviewForm=document.getElementById('review-form'); if(reviewForm)reviewForm.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(reviewForm);const p=db.products.find(x=>x.id===reviewForm.dataset.productId);const rating=Number(f.get('rating'));db.reviews.unshift({id:`v${Date.now()}`,productId:p.id,authorId:currentUser().id,rating,text:f.get('text'),date:'Just now'});if(f.get('reviewType')==='seller'){const s=user(p.sellerId);s.rating=Number(((s.rating*s.reviews+rating)/(s.reviews+1)).toFixed(1));s.reviews++;}saveDb();closeModal();render();toast('Review published', 'Thanks for strengthening the marketplace community.');});
  const authForm=document.getElementById('auth-form'); if(authForm)authForm.addEventListener('submit',e=>{e.preventDefault();const f=new FormData(authForm);const name=f.get('name').trim();const contact=f.get('contact').trim();const id=`u${Date.now()}`;const newUser={id,name,role:f.get('role'),location:'Sri Lanka',phone:contact.includes('@')?'Not provided':contact,email:contact.includes('@')?contact:'Not provided',initials:name.split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase(),color:'#d5e5b0',rating:5,reviews:0};db.users.push(newUser);db.currentUserId=id;saveDb();closeModal();state.page='home';render();toast('Welcome to HarvestLink!', `Your ${newUser.role} account is ready.`);}); }

function attachEvents(){ document.querySelectorAll('[data-nav]').forEach(el=>el.addEventListener('click',e=>{e.preventDefault();setPage(el.dataset.nav);})); document.querySelectorAll('[data-product]').forEach(el=>el.addEventListener('click',e=>{e.preventDefault();setPage('product',el.dataset.product);})); document.querySelectorAll('[data-modal]').forEach(el=>el.addEventListener('click',e=>{e.preventDefault();openModal(el.dataset.modal,el.dataset.productId||el.dataset.requirementId||null);})); const search=document.getElementById('search'); if(search)search.addEventListener('input',e=>{state.search=e.target.value;refreshMarketplaceResults();}); const cat=document.getElementById('category-filter');if(cat)cat.addEventListener('change',e=>{state.category=e.target.value;render();});const seller=document.getElementById('seller-filter');if(seller)seller.addEventListener('change',e=>{state.seller=e.target.value;render();}); const clear=document.getElementById('clear-filters');if(clear)clear.addEventListener('click',()=>{state.search='';state.category='';state.seller='';render();}); document.querySelectorAll('[data-requirement-detail]').forEach(card=>{const openRequirement=()=>openModal('requirement-detail',card.dataset.requirementDetail);card.addEventListener('click',openRequirement);card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openRequirement();}});}); const account=document.getElementById('account-button');if(account)account.addEventListener('click',()=>openModal('account'));const mobile=document.getElementById('mobile-nav');if(mobile)mobile.addEventListener('click',()=>document.getElementById('main-nav').classList.toggle('is-open')); }

function routeFromHash(){ const hash=window.location.hash.replace('#',''); if(hash.startsWith('product/')){state.page='product';state.selectedProductId=hash.split('/')[1];} else if(['home','market','requirements','dashboard'].includes(hash)){state.page=hash;state.selectedProductId=null;} else {state.page='home';state.selectedProductId=null;} render(); }
window.addEventListener('hashchange',routeFromHash); routeFromHash();

